export const metadata = { title: 'Create Account — COSMÉTICA' }
export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return <div style={{ marginTop: -80 }}>{children}</div>
}
