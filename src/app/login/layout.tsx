export const metadata = { title: 'Sign In — COSMÉTICA' }
export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <div style={{ marginTop: -80 }}>{children}</div>
}
